[pytest]
markers =
    perf: performance benchmark tests
    embedder: tests that require the Python embedder service
    indexer: tests that require the Rust indexer service
    slow: tests that run for more than 30 seconds
