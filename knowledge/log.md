---
type: Log
title: coderag knowledge history
---

# Bundle history

## 2026-08-26

- **Creation**: [a search writes one structured row, and the fleet log level stays where it is](decisions/a-search-writes-one-row-and-the-log-level-stays.md). `search.py` and `rank.py` held zero log calls between them, the whole engine held 14 call sites and no `log.debug`, and the level was hardcoded with no environment switch. So the pool-cut starvation was found by an offline replay that rebuilt the pool by hand. The record is `searchledger.py`, modelled line for line on `pinledger`: one JSONL row per search under `STATE_DIR`, one generation rotated by rename, every write best-effort, and `path()` resolved per call. A row carries the trace id, the caller, the root, the mode and every stage boundary with its own elapsed milliseconds, so `pool` and `cut` sit side by side. The error path writes the same row with the error string and returns the trace id for the caller to quote, where before it returned a string and wrote nothing anywhere. The reply never carries the stages. `CODERAG_LOG_LEVEL` defaults to `INFO`, so the fleet default does not move and the refusal in [a failure that resolved itself left no trace](defects/a-failure-that-resolved-itself-left-no-trace.md) still holds, re-measured at 5,716 lines in 24 h with 3,778 of them watcher `detected` lines. `coderag trace` reads the ledger back.

- **Update**: the sixth amendment to [the search unit is the caller's own workspace](constraints/the-search-unit-is-the-callers-own-workspace.md). `search` no longer calls `scope.enforce`, so a caller may name any registered, enabled and indexed project, and the registry row is the whole boundary. `index` still calls it, so enrolling a project still requires standing in it. The document's own argument is what retires the read-side pin: containment is not authorization on a localhost daemon, the fifth amendment already skipped the pin for `POST /register`, and the fourth already widened a member's reach to 142 projects it never named. The claim shrinks to the registry, the title says so, and the load-bearing live assertion flips: `test_live_scoping.py` now asserts that a named indexed stranger answers and that a path no row holds is refused with `not indexed`. `scope.observe` holds the `log.info` and the `pinledger.record` that `enforce` used to make as a side effect of refusing, and both surfaces call it, because a reader grouping calls by client cannot tell an absent record from an absent call. `resolution_note` gained a third silent arm in the same change: it says the target is the project containing the caller's workspace, which `enforce` used to guarantee and a named root breaks, and it shipped false on the first live call after the restart.

## 2026-08-25

- **Creation**: [the pool cut starved the members it was built to reach](defects/the-pool-cut-starved-the-members-it-was-built-to-reach.md), `status: resolved`. `pool_cut` was built for a root federating 135 members, with `limit` at `CANDIDATES` of 60. `federation.unit` then widened a member cwd to its root's whole unit, so a search from one federated leaf spans 358 projects. 337 members shared the 30 slots the caller's own half left, in the order the federation lists them, and 307 reached nothing. Replayed offline against the live stores: the leaf that answers the question produced 60 candidates and none survived the cut. Recall was never the fault, and a live run showed the same query with `path_glob="Services/*.php"` returning that file at rank 3. `limit` is a floor now, growing to `own_share + len(members)`, and `own_share` still comes off the original `limit` so the caller's own half does not inflate. Measured with the daemon stopped: retrieval over 358 projects is unchanged at 3.34 s, and the rerank goes from 590 ms over 60 chunks to 3792 ms over 367. That is 9.8 ms against 10.3 ms per chunk, so the cost is linear. The answer moves from absent to rank 8.

- **Creation**: [the content-hash skip is declined, because a store cannot see another project's duplicate](decisions/the-content-hash-skip-is-declined-because-stores-are-per-project.md). A remediation plan read `CREATE INDEX files_sha ON files(sha256)` as a half-built feature and proposed wiring it into a skip, which would make near-identical deployed refs almost free. It cannot. `config.index_path` gives every project its own sqlite file, so the duplicate the plan wants to skip is in a store this connection never opens, and the saving needs a store-external embedding cache that does not exist. `discover.plan` already skips an unchanged file by `known.get(rel) != meta.sha256`, path-keyed, over a full scan of `files`, so no index was ever the mechanism. The line is deleted: nothing queries it, nothing asserts the schema text, and there is no migration or schema version to break, because `connect` reapplies an `IF NOT EXISTS` script on every open. Measured first, because a decline needs the size of what it gives up: 4.3 GiB over 343 stores, of which the 143 worktree stores are 1147 MiB, and two sibling ref pairs share 95% and 92% of their file hashes. A revisit needs that cache keyed by the chunk hash plus the `embed_model` and chunker stamps `store.incompatible` already compares, which a model swap then orphans in full.

- **Update**: the daemon serves a third route, `POST /register`, and the fifth amendment to [the search unit is the caller's own workspace](constraints/the-search-unit-is-the-callers-own-workspace.md) carries why it skips the pin. `index` takes its root from the client handshake, so a SessionStart hook has no way to name the directory it stands in. The route takes the root in the body and calls `enroll`, which is the half of the `index` tool that runs after the scope check. It enqueues the walk rather than running it, because the caller is on the path of a session's first turn. Containment is not authorization on a localhost daemon, so this reaches nothing a `curl` did not.

- **Update**: the served instructions name the `ToolSearch` step. A client that defers a tool schema fails the first call, and every live run opened with that load. This is a factual step and not a fourth escalation of the prose, which [served prose does not beat grep](constraints/served-prose-does-not-beat-grep.md) measured three times.

- **Creation**: [the watcher went blind in two ways, and reported neither](defects/the-watcher-went-blind-in-two-ways-and-said-neither.md), `status: resolved`. A project dropped for a broken `.coderag.yaml` never re-armed, because the repair moves no registry row and `_intent` held rows. `_intent` now stamps each path with the mtimes of `.coderag.yaml` and `.coderag.toml`, and both count: `projcfg` refuses a leftover TOML file rather than ignoring it. A claiming root's config is not stamped, because `effective` suppresses a broken one there.

- **Update**: `_loop` catches `OSError` out of `_watch` now. That is where inotify raises at the per-user watch ceiling, and where a project deleted mid-pass raises. Uncaught it killed the thread, and `server._guarded` wraps the scheduler thread rather than this one. The failure lands in `watch.error()`, and `/healthz` publishes it under `scheduler_errors` as `watch`. Headroom today is 101,767 watches against a ceiling of 1,048,576, so this is a guard and not a fire.

- **Update**: `health.check` reads `watching`, the indexer queue depth and `projects_failing` against the truncated `failing` list. Those are three of the ten fields it published and never read. A fleet reconcile makes a deep queue ordinary. So the queue rule needs a depth at or above `HEALTH_QUEUE_STUCK` that did not fall since the last check. `coderag-doctor.timer` reports daily and pages nobody, with `SuccessExitStatus=0 1` and no `--prune`. The amendment to [a fleet alert decides on two samples](constraints/a-fleet-alert-decides-on-two-samples-not-on-a-count.md) carries the reasoning.

- **Creation**: [a member answered alone, and the reply read like the federation's](defects/a-member-answered-alone-and-read-like-the-federation.md), `status: resolved`. `search` ran `federation.expand(root)`, so a session in a member searched the member. On the live tree that is 1 project of 143 and 612 files of 57,864, and `searched.projects` is the only field that says so. The caller had no repair: `scope.enforce` refuses a member that names its root, because the root does not contain it. A live agent run searched six times, fell back to `find`, and reported code as not checked out when it was.

- **Update**: `federation.unit` is the search path. A member's unit is the member, then the federation of every enabled root claiming it. A root's unit is `expand`, unchanged, and a member of a disabled root answers alone. The caller's own project stays first, because `rank.pool_cut` gives the first project half the slots.

- **Update**: the cost of that widening, measured from `gen3-app-c` warm on one query. 0.65 s over 1 project against 5.5 s to 17.6 s over 143. A root call in the same federation already pays the wide figure, so this charges an existing cost to more callers. The narrow reading was the cheap one and it was wrong. The change widens what [the search unit is the caller's own workspace](constraints/the-search-unit-is-the-callers-own-workspace.md) claims, and its fourth amendment states the new boundary.

- **Creation**: [the index reply graded a project by one row of 143](defects/the-index-reply-graded-a-project-by-one-row-of-143.md), `status: resolved`. The audit went looking for an action that is not collective and found none. `register`, `submit`, `unregister`, the hourly `sweep`, the watch set and `expand` all take the unit to be the root together with its members. The answer was the exception. `indexed` was the root's own row, and that row is 33,053 chunks of the 185,453 the one federating root here searches.

- **Update**: `indexed` now sums the enabled rows of `[root, *members]` and names how many it summed. `root_indexed`, `pending`, `members_watching` and `member_errors` carry the per-project grain, which is where one stuck member is visible at all. `watching` stays per project. A member dropped for a broken config never joins the armed set, so an all-of-them predicate would read false forever. The live fixtures poll that field before they write, and they would hang out their timeout. `suppressed_by_inherited_excludes` stays per project too: it walks the candidate set twice, and 143 of those walks is a fleet-sized cost on a status call.

- **Update**: every hand-written page in this repo was rewritten against the wording rules the fleet checker grades, and no concept changed its claim. `ccw ste fix --write` corrected Rules 1.1, 1.14, 4.2 and 8.1 in place. Rules 5.1, 6.3 and 6.6 were rewritten by hand, because those are the three a PreToolUse hook denies a write on. The tracked corpus went from 627 refusing findings to 211, and all 211 of those are in this file, which is append-only history and is excluded for that reason. Outside it the count is 0, over `corpus_sha256` `b4b73be8d68253d20a626483fc8682f757311624588f36425ed24118e5f7e3d8` and `dictionary_sha256` `49c777026741bf0473a201bf194c08bed3d6a6d92d85d3a0c0e50bb2500ba7ed`, with `terms_skipped` false. Index glosses and the `description` they are copied into were resynced after the rewrite, because the two are one string in two places. Vocabulary findings (Rules 1.1 and 1.5) stay a reported number: the registry is a Rule 1.8 grant, not a mute button. Never read any of this as a claim of conformance to ASD-STE100 — the approved dictionary is not redistributable, and 44 of the 53 rules are not graded here.

## 2026-08-24

- **Creation**: [a dead row paged hourly](defects/a-dead-row-paged-hourly-and-nothing-could-remove-it.md), and this entry replaces the by-hand procedure the 08-23 entry below recorded. `registry.forget(keys)` and a `doctor --prune` that calls it are the removal path that did not exist; the gate is whether a live root still claims the row, not `last_error`, which the sweep clears. Twenty `/tmp` rows from a host's live tests had saturated `HEALTH_FAILING_CAP`, so the alert was full of junk and could not have carried a real failure.

- **Update**: [a dead row paged hourly](defects/a-dead-row-paged-hourly-and-nothing-could-remove-it.md) gains the half the first entry missed. A live run under the silenced SessionStart card still made ten rows: `search` itself says "call index(root='/tmp/…') first" and the model obeys. The engine cannot refuse a temp root anywhere — every fixture project in this suite is a `tmp_path` directory, so the refusal reds the suite — which puts the fix on the caller that knows its directory is throwaway. `coderag forget <path>` is that surface, and ccw's `livecap` runner now forgets its workdir in the same defer that deletes it.

- **Refused**: no concept for `forget` itself. It is four lines of loop under a docstring that already states its own why, and a bundle entry would restate the module docstring — which is what `log.md` exists to record instead.

## 2026-08-23

- **Update**: [a fleet alert decides on two samples](constraints/a-fleet-alert-decides-on-two-samples-not-on-a-count.md) gains what its first live page taught. Not a drill: the live lane claims roots under `tmp_path`, pytest deletes the directories, and the surviving rows fail at every sweep — so they cleared both samples and `cli health` exited 1 naming a `tmp_path` under `/tmp/pytest-of-*/`. Released both dead rows by hand (245 to 243) and pruned 24 unclaimed stores, 72 MiB; the fleet reads `156 projects, none failing` twice running. Removal stays explicit — `claim()` gets no path-shaped refusal, because `tmp_path` is where the registry's own tests claim.

- **Creation**: [the scheduler suppressed its own death](defects/the-scheduler-suppressed-its-own-death.md). Both scheduler jobs ran inside a bare `contextlib.suppress(Exception)` with no log call, and the sweep is what would have written a registry row — so a reconcile raising every hour left no traceback, no `last_error` anywhere on the fleet, and a green `/healthz`. Found by auditing the daemon's suppressions rather than by a failure: the alert shipped the same day could not have seen it. `_guarded` logs and records per job name, `/healthz` returns `scheduler_errors`, and the health check folds them in as `scheduler:<job>`.

- **Creation**: [a fleet alert decides on two samples](constraints/a-fleet-alert-decides-on-two-samples-not-on-a-count.md), and the wiring the defect entry below called outstanding is done. `OnFailure=` cannot see a daemon that is up, so an hourly `coderag-health.timer` runs the new `cli health` and reuses the existing alert unit. It pages only for a project failing at two consecutive checks: `last_error` clears on the hourly sweep, `last_error_at` is restamped by every failure and `error_total` never resets, so no single field separates a transient failure from a stuck project. `/healthz` returns the failing identities beside the count, because a count cannot tell one project failing twice from two failing once each. Live: the timer fired at 08:01, reported `155 projects, none failing`, and rescheduled for 09:01.

- **Update**: refuted — the alert unit was recorded as host-owned with no installer in this repo. `src/coderag/systemd.py` generates it and `cli install-systemd` writes and enables it; nothing was outside the repo. The alert's notification said "coderag down", which is wrong for a fleet that is up, and now names the unit that failed.

- **Update**: the scratchpad row is unregistered and its orphan store pruned, 244 rows to 243. No path-shaped refusal was added to `claim()`: pytest's `tmp_path` lives under the same `/tmp` and this repo's registry tests claim those paths on every run, so a rule refusing temporary roots would fail the suite that proves the registry works — and it is the same shape of predicate as the pruning `load()` that once wiped 236 rows. Removal stays explicit.

- **Update**: the chokepoint is now held by a test rather than by having grepped once. An AST walk asserts `record_error` is the only function that flags `last_error`; a fifth call site setting it directly would leave both durable counters behind. Confirmed live against an isolated `CODERAG_STATE_DIR`: a retired `.coderag.toml` raises `ConfigError` in the watcher, the row reads `error_total: 1` with `last_error_at` set and `armed: False`, and the pair survives the success that clears the string. Fleet registry untouched, 244 rows before and after.

- **Creation**: [a failure that resolved itself left no trace](defects/a-failure-that-resolved-itself-left-no-trace.md). `last_error` was a single overwritten string with no timestamp and no count, cleared by the next success — and `reconcile_all` supplies a success every hour, so any failure shorter than the sweep was erased before anyone could read it. `last_error_at` and `error_total` are never cleared, and the four sites that recorded a failure (indexer, `index` tool, watcher, federation sweep) now go through `registry.record_error`, which is the only writer. `/healthz` gains `projects_failing` and `errors_total`, because a liveness check stayed green through every project failing to index. Raising the log level was refused: 8,140 lines in 24 h at INFO is already the volume, and the defect was that errors were ephemeral, not unlogged. Pointing `coderag-alert@.service` at the new counts is outstanding — host-owned unit, no installer here.

## 2026-08-22

- **Fix**: the reply advisory was keyed on the wrong predicate and was false where it fired. `registry.enclosing` only ever returns an ancestor, so `resolved != pinned` means "the pin is a subdirectory" — whose files the answering project already indexes. `reach_census.py` now splits that class: **12,395 of 12,395 live upward resolutions are plain subdirectories, zero are their own checkout**, so the note was wrong on 79% of this machine's sessions and never reached the worktree it was written for. The predicate is now `.git` at the pin — a file in a worktree, a directory in a nested clone, absent in a subdirectory — and the test's load-bearing arms are the two silent ones.
- **Fix**: `test_a_live_thread_is_not_the_same_answer_as_this_project_being_watched` raced the watcher for `_armed`. `isolated_state` puts the registry under `tmp_path` and the test claimed `tmp_path`, so the live loop armed off the test's own registry writes rather than at the 5 s poll — the assertions read a global the loop owns. The loop is parked for this test; the real one is still exercised by `test_one_broken_config_does_not_take_the_whole_watcher_down`. 40 runs of the test and 5 of the unit suite under 24-core load, no failures; the guard still reds when `armed()` is made to return `watching()`.
- **Update**: [the search unit is the caller's own workspace](constraints/the-search-unit-is-the-callers-own-workspace.md), second amendment — the reach number was never a measurement of the change. 93.8% was `7.2 + 86.6` out of the *pre-change* census, the same arithmetic on the same data, so it was as true before the change as after: it is the ceiling the rule can reach, not evidence it reached it. `scripts/reach_census.py` is checked in because the figure survived a whole plan as prose in `scope.py` and two records without being re-derived once. Re-run against the current registry: 15,693 sessions, 244 rows / 156 enabled, **14.1% direct / 79.0% resolves-up / 6.9% unresolved**. Two ways the script can lie and does not: all five `~/.claude*/projects` are symlinks to one store, and the walk filters on `enabled` by calling `registry.enclosing` rather than re-implementing it. The residue is mostly irreducible — 713 of 1,087 unresolved are bare `$HOME`.
- **Fix**: `default_root`'s short-circuit read disabled rows. `registry.get` returns any row and `enclosing` filters on `enabled`, so a pin equal to a disabled row got a root with no store instead of the enabled ancestor that had one — silent, because an empty result set and a wrong root look the same. Zero of 15,693 sessions hit it; 88 disabled rows and a sweep that adds more is why it was worth closing. The fix is a deletion: `enclosing` returns the pin itself when the pin is enabled, so the short-circuit only ever differed where it was wrong.
- **Update**: the upward walk is disclosed in the reply, not only in a hook. `scope.resolution_note` fills the existing `hint` slot when the searched root is not the pin — `bridge.py` serves clients that get no SessionStart hook at all, and for those the walk was silent. Keyed on the roots disagreeing rather than on `.claude/worktrees`, which would hard-code one client's directory convention into the engine for nothing. Its test asserts the note is **absent** for a pin on a registered root as well as present for one inside it; the first assertion alone passes against an unconditional string.
- **Fix**: the sweep branch was in no test. `tests/test_watch.py` ran `_tick` once with `SWEEP_EVERY_S` left at an hour, so deleting `server.py`'s sweep block outright left the suite green — the shape `claude-code-workflows` records as `defects/a-gate-that-skips-is-not-a-gate.md`. Two arms now, because the fast one alone passes against an unconditional sweep. Item 2a of the plan is closed by production rather than by fixture: the daemon logged `sweep claimed 7 new members` and `/healthz` moved 149 → 156 projects, which is also the +7 the census independently counted.
- **Update**: `serve()` quiets `mcp.server.streamable_http` to `WARNING`. `Terminating session: None` once per `/mcp` request is 11,649 lines a day, and it was burying the hourly `sweep claimed N` — the only production signal this work has. Not a bug in the SDK: `stateless_http=True` means there is no session id to print. A level and not a message filter, which would break in silence at the next SDK release, and in `serve()` rather than `build_app()`, which the tests import.

## 2026-08-21

- **Update**: six concepts gained `sources:`. A fleet audit measured what the nightly `okf verify`
  timer can see -- it reads only concepts carrying an external source -- and this bundle had 49
  concepts and zero, so the largest bundle on the machine contributed nothing to the drift report.
  The six are the ones resting on a fact from outside this repo: five arXiv papers behind the
  chunking decisions, the MCP progress spec, and the three bug trackers the Dynamic Boost runbook
  is a workaround for. Those three are the interesting case -- the runbook exists *because* two of
  them are open, so their closing is the event that retires it, and nothing here would have noticed.
  The other 43 are derived from this repo and correctly cite nothing.


- **Update**: the checker pin moves to okf `v0.6.0`. Upstream tightened §5 on 2026-08-20 with no version bump — every timestamp-valued key is now an ISO 8601 datetime with an explicit UTC offset — and we found it a day late by cloning the spec repo, which is the more serious half: nothing would have told us. `TimestampsCarryAnOffset` covers the six keys that carry an instant and lands in `-strict` for one tag while the fleet converts; a value with no offset is now skipped by the staleness comparison rather than guessed at as midnight UTC, matching the reference implementation. `okf check -against <ref>` reads each concept's previous version out of git and reports a dropped or reordered heading, a lost `sources` or `verified` entry, or a rewritten `type`/`title`/`resource` — the bundle skill's "augment, never shrink" rule, which until now was prose enforced by nothing. `okf viz` renders the bundle as one self-contained page, no CDN and no vendored library; its output is generated on demand and gitignored, because a committed rendering is stale the first time a concept changes.
- **Creation**: [the bundle gate runs before the push, not after](decisions/the-bundle-gate-runs-before-the-push-not-after.md). This repo had no git hook at all — `core.hooksPath` unset, `.git/hooks` holding only samples, no `.githooks/`. CLAUDE.md named `uv run pytest tests/test_okf_bundle.py` as the gate and nothing ran it on commit or push, so CI was the only enforcement and it answers after the change is already on the remote. `.githooks/pre-push` runs `okfrules check -Werror knowledge` and fails closed on a missing checker; three tests mirror its self-assertions. The refusal probe was verified against a stub `okfrules` that exits 0 on everything — the hook blocks it — and the `-Werror` arm against a dangling link, which plain `check` passes. Writing this concept is what caught the first real red: the gate refused it as an orphan until it was in `decisions/index.md`.
- **Update**: the checker pin moves to okf `v0.5.3`, which closes the last unbuilt item of the stamping plan. §5.5 makes staleness a plain date comparison, so `stale_after` says nothing at all until the day it says red, and a passing check that advanced the date would be silencing the calendar rather than answering it. `okf verify` now counts down instead: inside 30 days it prints `warning: stale in N day(s)` and leaves the exit code alone, so a bundle learns about the cliff a month before it falls off rather than on the morning its CI turns red. Nothing else moved — `okfrules check` is byte-identical at this tag.
- **Update**: the checker pin moves to okf `v0.5.2`. Four fleet sources are issue trackers and directory listings whose HTML differs on every request, so the digests recorded at `v0.5.1` never matched twice and the check reported drift forever — noise in the one place the check was meant to be worth having. A digest is now recorded, and a mismatch called drift, only after a second fetch agrees with the first; a source that fails that proves reachability and nothing more, which is all it had ever proved. One `generated.at` here was dated ahead of the time it was written and now reads the commit's own.
- **Update**: the checker pin moves to okf `v0.5.1`. It adds `okf verify`, drops `VerifiedWellFormed`'s demand for a `human:` actor — that demand made §5.3's machine tier unreachable, and the anti-forgery guard moved into the writer, which can see `CLAUDECODE` where a document rule cannot — and adds `SourceHasAResource`, which holds §5.1's one required key within a `sources` entry. One pin for the fleet is the point, so this repo moving late would read as drift in `okf sweep` rather than as a repo holding back deliberately.
- **Fix + Creation**: [a derived column was stored and never re-derived](defects/a-derived-column-was-stored-and-never-re-derived.md), found by the scan that was supposed to confirm the entry above it. `files.lang` is derived from the path and written once, and the content-hash diff never rewrites a file whose bytes did not change — so widening `LANGS` 40 → 166 reclassified **zero** of the 2,058 `.groovy` files already indexed. The no-language bucket did fall, 14,639 → 6,506, and every file that moved moved because it was *deleted* from the index; `.svg` leaving covered for the reclassification that never happened. `index._relang` re-derives on every pass and updates the rows that disagree — no chunk touched, so no embedding and no GPU. After it: **3,782**, against a prediction of under 3,000 — a miss, and see the audit entry below for why the first reading of it was 2,077. The general rule is not about `lang`: any column computed from this repo's own tables is invisible to a diff that asks what is on disk.
- **Verified**: the three predicted numbers, measured on the fleet after a full reconcile. Nested-ignored files 278 → **0**, the one prediction that held. No-language files 14,639 → **3,782** against under 3,000 — a **miss**. Sub-line chunks 10.12% → **4.64%** against under 4% — a **miss**. The sub-line residue is json 12.1%, javascript 13.6% and html 21.4%, none of which this work touched: it deleted generated content and changed no splitter, so 4.64% is what filtering alone reaches and the rest needs the JSON scope header that is deliberately unbuilt.
- **Fix**: three more from a second audit, all of them interactions between tables that were each correct alone. **(1)** The root-anchoring defect was fixed for directories and left standing for whole filenames — `package-lock.json`, `go.sum`, `gradlew`, `.terraform.lock.hcl` and twelve others were globs in an anchored list, so 27 nested files and 1,290 chunks of dependency graph were indexed, and the fix that closed the directory half had *added fifteen more* inert entries. `ignores.IGNORE_NAMES` is the symmetric set; `DEFAULT_IGNORES` holds globs only and a test asserts it. Written back into [the ignore list only ever matched at the root](defects/the-ignore-list-only-ever-matched-at-the-root.md), which is where it belongs — it is the same bug in the same list. **(2)** `.vhd` was in `_BINARY_EXT` as a disk image and in `LANGS` as VHDL, and `is_binary_ext` gates indexing, so VHDL source was refused. Removed from the binary set: a real disk image is caught by the NUL sniff, and a wrongly-refused source file is caught by nothing. **(3)** `lang_of` read the suffix before the whole name, so `CMakeLists.txt` resolved through `.txt` to `docs` and the `FILENAMES` entry for it was unreachable. Whole name first now. Each has an invariant test rather than a case test — no extension in both `LANGS` and `_BINARY_EXT`, no whole filename in the glob list — because the case that was wrong is the one already fixed.
- **Update**: `.local` removed from `LANGS`, found auditing the commit above against `linguist/languages.yml` rather than against the plan. Three of the 166 extensions are in no linguist language — `.comp`, `.conf` and `.local` — and `.local` was the one that mattered: the 1,740 files it labeled `ini` are nginx server blocks, plus a `jail.local` and a `Caddyfile.local`. It also carried the headline. The first scan read **2,077** no-language files and cleared the under-3,000 prediction; without the mislabel the true figure is **3,782** and the prediction misses. A wrong label is worse than none — it puts nginx config inside `lang="ini"` and still fails anyone searching for nginx. `.conf` (191 files, every one a config file) and `.comp` (a Khronos compute-shader suffix linguist omits while carrying every other stage) are kept as deliberate non-linguist entries and say so in the source. Eight extensions linguist *does* give INI and this table lacked — `.cnf .service .socket .target .timer .mount .network .container` — went in. `_relang` reverted the 1,740 rows to unlabeled with no re-embedding, which is the backfill working in the direction that removes a classification.
- **Fix + Creation**: [the ignore list only ever matched at the root](defects/the-ignore-list-only-ever-matched-at-the-root.md). `fnmatch` anchors, so every directory entry spelled `node_modules/*` excluded exactly one copy — the top-level one — and 278 of 70,218 indexed files sat under a nested copy of a directory the list claims to exclude. `ignores.IGNORE_DIRS` is 77 bare names tested against `Path(rel).parts`; `DEFAULT_IGNORES` keeps the globs and keeps the anchored reading, which is correct where it shares a matcher with a project's own `exclude:`. Both moved out of `config` into `ignores.py` — data, not knobs, and a third of that module. No store invalidation and none should be added: `discover.changed()` diffs present-against-stored, so the newly-excluded paths land in its delete list on the next pass.
- **Fix + Creation**: [the secret filter had a dash-shaped hole](defects/the-secret-filter-had-a-dash-shaped-hole.md). `*.env` needs a literal dot, so `laravel-env` was indexed with **287 value-bearing `KEY=value` lines**, `env-template` with 197 and `.env-example` with 9. `*-env`, `*-env.*`, `env-*` and `*.env-*` close it, and `_SECRET_EXEMPT` gains the dash spellings so the two templates index on a decision rather than by being missed. `shhgit`'s `settings.py`/`config.php`/`database.yml`/`*.sql` entries were refused: they are source, and `gitleaks` shipping no filename denylist at all is the same judgment from the other end.
- **Update**: the extension lists filled out against linguist and the two scanners — `_BINARY_EXT` +~70, `_IMAGE_EXT` +20, `LANGS` +~120, `_SECRET_NAMES` +~20, plus a new `filters.FILENAMES` for the 1,760 extensionless files. `.svg` moves to `_IMAGE_EXT`, reversing the earlier reading that SVG is markup: 8,039 files, nearly all generated icon sets, and the largest single source of chunks with no line break in them. `.mod` was refused from `_BINARY_EXT` — `Path("go.mod").suffix == ".mod"`, and the fleet holds 17 of them.
- **Deprecation**: [the thermal pause is not what indexing costs](decisions/the-thermal-pause-is-not-what-indexing-costs.md). `gpu.cool_down()` is deleted at the user's instruction, and the profile in that concept is not what reversed it — 0.04% of self-time still holds, and that is the argument: costing nothing is also saving nothing, while what it *did* cost was a config knob, a `/healthz` field, a systemd `Environment=` line, a test module and two concepts. `elapsed_s` is uncorrected wall clock now; `cooled_s` is gone. Throttling is the driver's, and the latency constraint says so instead of naming a function this repo no longer has.
- **Update**: three additions to [one chunker and it is third-party](decisions/one-chunker-and-it-is-third-party.md), because the question has now been asked five times. `CodeSplitter` **exists in the pinned wheel**, undocumented on PyPI, and is declined on purpose rather than overlooked; this library is not LangChain's `RecursiveCharacterTextSplitter` — it binary-searches for the largest rung that fits and never splits below a character, so langchain#10410 and #9305 do not transfer; and arXiv:2608.16586 (Fisher randomization ×10,000, Bonferroni over 28 comparisons) finds no dominant strategy, on prose, so it corroborates without governing. Measured alongside: sub-line chunks are **0.0%** in go/typescript/java/xml and 27.8% in the unlabeled bucket, 10.12% overall and every point of it generated content — the boundaries are a filtering problem, and any splitter swap argued on that number is aimed at the wrong module.
- **Refused**: a JSON key-path scope header and a `max_per_file` A/B are recorded in that concept as open, not built. The header would be the file-path trick one level deeper for 25,629 JSON chunks, and it is unbuilt because the file-path arm is the only header arm with evidence — building a second on none is what the derived-header line cost once already.
- **Update**: `filters.DATA_LANGS` deleted, unreferenced since the header-dispatch table it was written for was reverted; `search.py` split at 332 lines, with `pool_cut`, `diversify` and `Hit` moving to `rank.py` and `rrf` staying beside the two rank lists it fuses; `tests/test_public_hygiene.py` now enforces the 300-line ceiling `CLAUDE.md` had only ever stated.
- **Update**: the checker pin moves to okf `v0.4.1`, where `ActorConvention`, `StatusVocabulary`, `FootnoteLabelsJoinSources` and `AttestedComputationHasContract` are `Standard()` rather than `Strict()` — the fleet conversion they were staged behind finished and all ten bundles measure zero, the same promotion condition `NoIntraBundleWikilinks` met at v0.2.1. One pin for the fleet is the point, so this repo moving late would read as drift in `okf sweep` rather than as a repo holding back deliberately.
- **Update**: the index is the reference shape (§8). Every one of this bundle's 4 directories gets its own `index.md`, headed by the `type` verbatim and singular, entries `* [Title](file.md) - description` sorted case-insensitively by title; the root lists directories and nothing else. It listed all 45 concepts behind one file, which is the disclosure problem §8 exists to solve. The shape is taken from `bundle/index.py` in the cloned reference rather than from §8's prose, because `index.md` is regenerated on every run there and never hand-authored — so the generator is what "ideal" means, down to `[dir](dir/index.md)` where §8 writes `[dir](subdir/)`. `log.md` stays unlisted; the root keeps its `okf_version` and its preamble. The empty `interfaces/` directory is deleted rather than given an index for nothing.
- **Update**: the vocabularies the spec closes, measured against the cloned reference bundles rather than against a reading of it — `status` is one of §5.4's three values (`resolved` ×12, `fixed` ×3, `refuted` ×2, `accepted` ×1, `active` ×1 moved), and an absent one already meant `stable`. okf `v0.4.0` is what measures them: its `-strict` arm carries `ActorConvention`, `StatusVocabulary` and `FootnoteLabelsJoinSources`, and this bundle is now zero on all three.
- **Creation**: [the guard read a variable nothing set](defects/the-guard-read-a-variable-nothing-set.md). The publishability sweep had been inert twice over — wrong variable name, then wrong separator — and the second shape passes green. Three tracked `knowledge/` files were redacted on the first armed run: a device-scale member count, a corpus name and a `pytest-of-<user>` path. `terms()` now refuses a token carrying `:` or `;`. Not written as a concept: the stale `REQUIRE_CLIENT_ROOTS` comment deleted from `config.py` in the same commit, which claimed the unit ships a `0` override that `407f125` removed — the flag's history is already in [the pin rollout](decisions/the-pin-rollout-needed-a-reason-not-a-count.md) and a comment restating a deleted line is the thing the ceiling exists to stop.
- **Update**: the checker's module path changes and nothing else does. `okfrules` and `okf` merged into one module at okf `v0.3.0` -- the fleet rules are now the package `okf/rules` and the binary is built from `cmd/okfrules` in the okf module -- so the CI step and `tests/test_okf_bundle.py` install from the new path. Same binary name, same `Standard()` rules, same verdict on this bundle: `check -Werror` is silent before and after. The old module's tags stay resolvable on the proxy, so nothing pinned to them breaks.
- **Update**: 28 entries in this file lose their improvised verbs for the five `LogVerbs` allows — `Added`/`Add`→Creation, `Correction`/`Corrected`/`Fix`/`Rewrite`/`Extension`→Update, `Deletion`→Remove, `Audit`/`Verification`→Verified. The bold lead is a label, not a claim about what happened, so a rename falsifies nothing; the entries that *are* claims the vocabulary has no word for — Refused, Refutation, Measured, Not changed — are left alone, which is what keeps `LogVerbs` opt-in. This bundle carried the largest share of the fleet's 57 renameable entries. Its gate is unchanged: `okfrules check -Werror` runs `Standard()`, which does not include the rule, so the rename is preparation and not a fix for a red.
- **Creation**: [doctor --prune globbed fresh directories against a stale row snapshot](defects/prune-raced-a-store-the-daemon-was-writing.md), the most serious of six defects found auditing two handed-forward findings. `registry._held` yields the rows with the lock still held, so the rows and the glob come from one view; `prunable_stores()` deletes under `LOCK_EX`; and a candidate written to within `CODERAG_PRUNE_MIN_IDLE_S` is kept, because a job queued before its row was deleted still indexes into a store no row holds. `--prune` exits non-zero now — the old loop `continue`d past `problems += 1`, so a run that deleted gigabytes reported `0 problem(s)`. The race test was seen passing on the unfixed code first and fixed until it went red.
- **Creation**: [1768 zero-root pins were an answered-empty case](defects/empty-root-answers-from-clients-nothing-identifies.md). The suspected cause — session-scoped capabilities on a stateless server — was tested live and **refuted for Claude Code**: a rootless search from 2.1.235 logs `1 root(s)` and a foreign root is refused. So instrumentation ships and the code change does not: the branch is carried in a `ContextVar` and logged with the client's name and version.
- **Creation**: [watch.armed() answered for projects the watcher refused to watch](defects/armed-reported-the-unfiltered-set.md) — `_armed` is the filtered tuple now, while the rearm comparison stays unfiltered for the reason recorded in the deleted-file defect.
- **Creation**: [a committed file symlink was read through in the git lane](defects/the-git-lane-read-through-a-file-symlink.md). A content escape: the default lane indexed the link's target and attributed it to the containing project. The only existing symlink test used a directory link in a project that was never `git init`-ed, so it covered the other lane.
- **Creation**: [per-project ranks were truncated as if comparable](defects/per-project-ranks-were-truncated-as-if-comparable.md). Normalizing the scores was the obvious fix and is **refuted** — RRF output is a rank, so every project's first hit already scores the same. A quota into the pool is what works. Both halves are invisible at `projects: 1` and load-bearing at 136, which is the fleet's one federating root.
- **Update**: `tools` no longer claims a root before `federation.register` validates it, so a project whose config cannot parse leaves no row for reconcile to retry forever. `FORBIDDEN_ROOTS` splits into exact members and `FORBIDDEN_TREES` walked through ancestors — `/`, `~` and `/tmp` stay **exact**, because every project is under the first two and the live suite indexes throwaway repos under the third.
- **Update**: `conftest.fleet_unchanged` compares a `fleet_digest` from `/healthz` — sha256 over every row's key, enabled, direct, roots and error state — instead of one integer over the enabled subset, which was blind to cancelling pairs, same-count-different-set and row-content drift. Unclaimed stores are reported as a **warning**, not a failure: disable-never-prune leaves them by design, so failing on them would red every run.
- **Update**: [the pin rollout](decisions/the-pin-rollout-needed-a-reason-not-a-count.md) — the five-profile criterion is replaced. It was unreadable as instrumented, and the five profiles' MCP entries are kept byte-identical by an installer, so there was never a per-profile variable to observe.
- **Update**: [the search unit is the caller's own workspace](constraints/the-search-unit-is-the-callers-own-workspace.md) — the design's load-bearing claim had no test in either direction; `tests/test_live_scoping.py` carries it, including the exact boundary that a member is reachable through its root and not nameable directly.
- **Update**: [doctor only walked the registry](defects/doctor-only-walked-the-registry.md) — its safety argument leaned on "unflagging keeps the row", which is false; the restart lane was miscredited with the orphans; and the 143-store backlog is now zero.
- **Verified**: 308 fast, 32 live, bundle 4. The prune race went red on the unfixed walk first, and so did every one of the seven other new guards. `fleet_unchanged` was proved to discriminate by a throwaway live module that skipped its teardown -- `{'projects': (148, 149), 'fleet_digest': ('5d9d32…', '1e3174…')}` -- and disabling that row returned the digest to its baseline while leaving the store, which is the generator mechanism reproduced on demand. Read-only from the fleet's one federating root: `searched.projects: 136`, 64,510 files, 213,704 chunks, members' own paths in the results, and **four** distinct projects inside a top-10, where the flat cut spread those ten slots across ten projects. The live lane then left 28 unclaimed stores; `--prune` took 21 and kept 7 as written-to-within-60s, which is the idleness floor doing its job rather than a miss.

- **Update**: the checker pin moves to `okfrules` v0.2.1, where `NoIntraBundleWikilinks` joins `Standard()`. It was `-strict`-only while one fleet bundle still carried bare wikilinks; that conversion finished and all ten bundles measured zero, so the rule now runs in every pinned repo rather than only the two that build their own checker. `Strict()` adds `LogVerbs` alone. This retires the last sentence of the entry below it: the gate here can now say the bundle is clean of wikilinks, and the 13 log verbs outside the vocabulary no longer stand in the way, because they were never `Standard()`'s to red.
- **Update**: the bundle's 13 intra-bundle wikilinks become relative markdown links. Nothing dangled; what the syntax hid was the next target to go away, because a checker cannot see inside `[[...]]`. The gate here runs `okfrules check`, whose `Standard()` set does not carry `NoIntraBundleWikilinks` -- that rule is `-strict`, and `-strict` also carries `LogVerbs`, which this log's 13 verbs outside the vocabulary would red. So the bundle is now clean of them and the gate still cannot say so; converting first is what makes the rule adoptable at all.

## 2026-08-20

- **Update**: CI and `tests/test_okf_bundle.py` run `okfrules` (`@v0.2.0`) rather than stock `okf` — the same conformance check plus the rules the fleet keeps and OKF §11 forbids a conformant consumer from enforcing. Two tests go with it: the hand-written link walk (the `-Werror` step is that check) and the hand-written `resource:` walk (now `ResourceResolves`, which parses the frontmatter instead of splitting a line on commas). Both were written here because nothing shared existed; keeping them means one bundle's rules diverging from the ten.
- **Creation**: [the bundle gate asserts that it is wired](decisions/the-bundle-gate-asserts-that-it-is-installed.md) — the five existing arms grade the bundle and all pass in a repo where CI has stopped running the file, so a sixth reads `ci.yml` and requires the step that invokes these tests, the strict `-Werror` check, the pinned install, no `continue-on-error:` key, and a trigger that fires on a change. Proved by excusing the step and by repointing it at another file. The module docstring's claim that this repo has no CI and no hook was false and is gone.
- **Update**: [joining a root is a config change](constraints/joining-a-root-is-a-config-change-to-an-existing-project.md) — the per-project config is **YAML** now (`.coderag.yaml`, `yaml.safe_load`), and the signature is why that was cheap: it hashes the *parsed* values, so the documented shape digests to `633d0eb003bb8a5b` under both parsers and none of the 256 rows reconciled. Three files on disk carried the old name. A leftover `.coderag.toml` is **refused**, not ignored — ignoring one drops its excludes and the only symptom is a store that grew — and `tools` already turns that into a `last_error` rather than taking the daemon down. Three shapes TOML could not express now have to be decided rather than defaulted: an empty document is `None` and means defaults, a non-mapping document is fatal, and YAML 1.1's `no`/`on`/`off` arrive as booleans, so an unquoted pattern that is one of them fails the type check instead of excluding nothing.
- **Creation**: [nine nodes run on the CPU EP in both exports](constraints/nine-nodes-run-on-the-cpu-and-that-is-the-design.md). The module docstring claimed four assertions; two had zero call sites and the fourth reads `get_providers()[0]`, which ORT fills with CUDA first whatever happened per node. Measured: both exports place **9 of ~960 nodes** on the CPU EP, identical ops, **0.059%** and **0.184%** of node time. `session.disable_cpu_ep_fallback` was the obvious mechanism and is **refuted** -- set, it refuses both models at load over exactly those nine. `check_placement` ships instead, two halves because either alone is defeated: `Gather` is both shape plumbing and an embedding lookup, so the allowlist alone hides a whole lookup, and a time bound alone never names the op that moved. `check_device` deleted (guarded a device parameter that exists nowhere) and replaced by a sufficiency test; `assert_gpu_available` wired into `cli._serve`, before the socket.
- **Creation**: [a deleted file stayed searchable in a federated member](defects/a-deleted-file-stayed-searchable-in-a-member.md) — two independent causes, either sufficient. inotify keys a directory by inode, so a root and the member it reaches through a symlink collapse to one watch descriptor and events arrive under whichever path registered last; and `_tick` re-armed the whole set every 60 s, which is **5.4 s over 151 projects and ~120,000 watches**, measured — a tenth of the watcher's life blind, and inotify replays nothing. The module docstring already carried the *other* symlink trap and it is not this one. `WATCH_POLL_MS` separates surfacing from Rust (free) from re-arming (not), and the comparison is against the **unfiltered** root list: a project dropped for a broken config is still enabled, so the filtered list differs every tick and re-arms forever.
- **Creation**: [a released member kept the excludes of the root that released it](defects/a-released-member-kept-the-roots-excludes.md). Narrowing is applied on join and widening was applied on leave by nothing at all. The resubmit fix then still did nothing, because `unregister` reported only the rows it **deleted** and `registry.release` returns True only on an outright delete — so the member that survives the release, which is the one that stays searchable, was the one it never named. The live test went on timing out for 300 s with the fix in place. The unit test claims the member directly, because without that the correct behaviour is to submit nothing and the test passes broken.
- **Creation**: [served prose does not beat grep](constraints/served-prose-does-not-beat-grep.md). A negative result, recorded rather than tuned away: three escalations of the served `instructions` and the tool description all lost to `grep` on a literal-string question in a navigable repo, with the injection verified end to end. The agent layer was rebuilt around a federated fixture — a root whose only interesting content sits in a member behind a directory symlink, which `grep -r` does not follow — and the session calls the server first time. Two client facts worth the same weight: `--allowedTools` **pre-approves and does not restrict** (a session given one tool still used `Bash` and `Read`), and a `tool_result`'s `content` arrives as a **JSON string**, not the protocol's list of blocks.
- **Update**: `build_app` nests the SDK's lifespan instead of replacing it, and runs `stateless_http=True`. Assigning over `lifespan_context` meant the session manager's task group was never entered, so every `/mcp` call answered 500 while `/healthz` stayed green — the daemon read as up from every check that was not a request. Stateless is also what makes the `2026-07-28` per-request envelope reachable; the stateful path negotiates only up to the last handshake revision.
- **Update**: the live harness. `Rpc` performs its own handshake lazily, so `-k` on a single test is a valid run rather than one that fails on a session ID some earlier test happened to establish; one transport-only retry covers the server closing an idle keep-alive connection mid-poll, and it deliberately does not retry a *response*, which is what would hide a real intermittent fault. `require_clear_gpu` counts VRAM **held by the daemon under test** as headroom: the process check already excludes it, and the first live search loads 12 GB, after which every later module read the suite's own working set as somebody else's.
- **Update**: the exclude fixture, and the lesson under it. Its vendored file was `vendor/bundle.min.js` — and `vendor/*` and `*.min.js` are both in `DEFAULT_IGNORES`, so no root's exclude was ever what dropped it: one test asserted a mechanism that could not have run and two waited 300 s each for a file the indexer is never allowed to see. **An exclude test's fixture must be a path the defaults would have indexed**, and `test_0` now asserts exactly that, because a future addition to `DEFAULT_IGNORES` would silently retire four tests otherwise.
- **Update**: the installed unit. `OnFailure` moves from `[Service]` to `[Unit]`, where systemd reads it — misplaced, it logs "Unknown key name" and carries on, so it ran for weeks as an alert that never fired and never said so. And `Environment=CODERAG_INDEX_TEMP_C=84`: the governor defaults to off, and the unit is the unattended overnight run on a card that throttles at 87 °C. It costs 0.04% of run time, already measured.
- **Update**: `tests/significance.py` — the paired reading of a bake-off, from the ranks `eval.py` now emits rather than from the four decimals. Exact-binomial McNemar over the discordant queries, a bootstrap CI that resamples *queries* so the pairing survives, and Benjamini-Hochberg because five arms against one baseline is five tests. Its own tests are hand-checked against a closed form (5 discordant pairs one way is p=0.0625) and include the two ways this reads wrong: an arm against itself must come out at exactly zero with no spread, and a rank of 11 in a stored run must not count as a hit at k=10.
- **Verified**: `pytest -m live` is **26 passed in 45 s**, including the five tests that had never been run — the three scripted-agent tests and the `2026-07-28` negotiation and conformance checks. The CPU suite is 259 passed. Four defects in this list were found by running the suite, not by reading the code, which is the argument the plan made for running it before the fleet index rather than after.

- **Refutation**: `MarkdownSplitter` loses its arm. 0.8033 against 0.8133 recall@10 over 300 doc queries on a public repo — −0.0100, CI [−0.0300, +0.0100], 3 won against 6 lost, p = 0.51. It removed every broken code fence and mid-table start in the 50-file sample and no retrieval metric could see it, so `CHUNK_MD_SPLITTER` stays off and [one-chunker-and-it-is-third-party](decisions/one-chunker-and-it-is-third-party.md) is strengthened on the corpus it was never argued over. The boundaries a reader calls wrong are not the boundaries that lose queries.
- **Update**: [the header dispatches on type](decisions/the-header-dispatches-on-type-the-splitter-does-not.md) cited arXiv:2605.00318's 0.366 → 0.754 as "measured on CSV and Excel". It is not: the benchmark is MAUD, SEC merger agreements reshaped into row/key-value form, and CSV/Excel is the abstract's motivating domain. The delta also bundles a row tree, key-value block encoding and greedy merging with a 40–56% cut in chunk count, so none of it is attributable to prepending a key path. Second citation in this bundle to claim more than its source — the first is recorded in [one-chunker-and-it-is-third-party](decisions/one-chunker-and-it-is-third-party.md).
- **Update**: `CHUNK_HEADER` becomes `CHUNK_HEADER_PATH` and `CHUNK_HEADER_DERIVED`, both stamped. One switch removed the path and the derived line together, and **48.6% of doc queries have their heading echoed in the positive's filename** (17.6% on code), so `no-header` scored the derived line and a filename identity shortcut as one number — and the derived line is the half F0 found unevidenced. Two booleans span the 2×2 exactly; a retained master switch would be a third knob for four states. The path's contribution is not a harness defect to correct: a real query legitimately matches a filename and the path is in the chunk in production. Only the reading was wrong.
- **Creation**: [a deliberate stop was indistinguishable from a crash](defects/a-deliberate-stop-was-indistinguishable-from-a-crash.md). `uvicorn.run` never returned — MCP streamable HTTP holds its stream open and graceful shutdown waits for it — so `_shutdown_exit`, the `os._exit(0)` that exists so the CUDA EP cannot abort with 134, was dead code. Every stop took 90 s, ended in SIGKILL, and fired `OnFailure`, which is how the alert that matters gets muted. Neither existing restart test could see it: their `Daemon.stop` kills after 30 s and reports success, so a helper that recovers from the defect made every test above it blind. The new test signals the process directly, holds a live stream open while it does — without that there is nothing to wait on and the defect does not reproduce — and asserts exit code 0 rather than absence.
- **Measurement**: the idle daemon, on a quiet machine, models resident, no index running: **0.20 CPU-seconds over 30 s (0.7% of one core), 77 threads, 1,688 MB RSS**. The previous engine's spin-wait finding does **not** transfer as an idle cost — ORT's intra-op pool spins during a `Run` and then parks, so a daemon sitting on a desktop costs nothing. It is a cost of the index pass, and it is still unmeasured here. What the number does indict is 77 threads (two ORT sessions with `intra_op_num_threads` unset, on 24 cores) and 1.7 GB resident. The 15.7 GiB of VRAM is not a defect: `MODEL_IDLE_UNLOAD_S` is 900 and the daemon had been up five minutes.

## 2026-08-19

- **Refutation**: [the chunk budget and the token window do not fit each other](defects/chunks-are-cut-before-the-model-sees-them.md) is closed by its own two arms. Widening the embedding window past the p95 moved recall by **nothing** — 0.1600 / 0.3200 identical to four decimals — for double the tokens per chunk, and cutting the chunk to fit the window was **worse** at both k. The 70%-truncated measurement was true and the inference from it was wrong: the discarded tail carries no retrievable signal and the lexical lane indexes it anyway. Both constants stay, the freeze the file placed on them is lifted, and `gte-win1536` is on record as a cost with no benefit — the more useful half, because widening the window is the change someone proposes next.
- **Update**: `is_secret_path` gains `*.env` and `*.env.*`. `fnmatch` anchors the whole name, so the single `.env.*` pattern only ever matched the *leading* form — `.env.local` yes, `prod.env` and `svc.env.enc` no. A sweep of the 148 enabled fleet roots found **456 credential-shaped tracked files** passing the filter, 352 excluding templates. Checked before calling it an incident and it was not one: **zero** carried a non-placeholder value. Closed before the fleet index rather than after, because that run is unattended across 148 repos and this filter is the only thing between a future committed credential and a store that answers natural-language queries. Re-swept after: 0. The guard names its five shapes individually — a test that reads the tuple it guards agrees with an empty tuple.
- **Update**: `*.ipynb`, `*.csv`, `*.tsv` join `DEFAULT_IGNORES`. Not a chunker problem, so not a chunker fix: a notebook is JSON holding base64 output blobs and escaped `\n` that are not newlines, so the splitter's top rung — runs of blank lines — does not exist in it and every cut lands mid-object. Neither shape yields a retrievable chunk under any splitter, and a format-aware one would only make the result tidier. Extracting a notebook's code cells is a real feature and is not this.
- **Creation**: [an eval query is removed from the corpus it is hunting](constraints/an-eval-query-must-not-be-findable-by-identity.md), plus `--corpus docs` and the harness's first tests. `eval.py` produces every retrieval number this repo has and had no test at all. The prose half was excluded from the query set by design and the cost of that went unstated: it made every prose-side change unfalsifiable, which is what let a header ship for months on a citation that said something else. Doc queries are the first heading plus its lead paragraph, stripped from the indexed copy; measured yield 18/19 here and 17/33 in ccw, against an earlier 82% estimate -- half that repo's doc files are a title followed straight by a sub-heading, and a three-word title is refused rather than padded. Two chunker arms land with it: `md-splitter` (`MarkdownSplitter` for doc-langs, off by default) and per-query ranks emitted alongside the aggregates, since 0.02 recall@10 is ~7 queries in 300 and the paired outcome is what a bootstrap CI needs.
- **Update**: `chunk_algo` and `chunk_md_splitter` are stamped into store meta and compared by `store.incompatible`. `ProjectConfig.signature()` versions excludes and nothing versioned the header, so changing what `scope_header` emits rewrote every embedded string while every store on disk still read as current -- stale in the one direction nothing reports. Free to fix today because nothing is indexed yet; a full re-index of 148 projects later.
- **Update**: four sites put in quotation marks that arXiv:2605.04763 calls 2,000 non-whitespace characters "a robust default". The paper says no such thing — its finding is that **chunk size has "a weaker, non-monotonic effect"**, and it names no default. The non-whitespace *unit* is cAST's (arXiv:2506.15655), cited correctly elsewhere. Two neighbouring numbers were wrong too: the function-chunking loss is 3.57–5.64 pp, not 3.96–5.64, and cAST's +4.3 Recall@5 is over *naive line-based* chunking, which is the sentence that makes our tie with it legible rather than surprising. A fabricated quotation in a public repo gets corrected whether or not the conclusion survives — and this one survives in better shape: 2,000 is a round number in a flat region, which is a weaker claim and a true one.
- **Creation**: [per-type knowledge enters through the header](decisions/the-header-dispatches-on-type-the-splitter-does-not.md). `scope_header` was three regex arms tuned to the C family, applied to a corpus that is 6.3% doc-langs, 9.4% structured data and 25% unlabeled. It now dispatches on `lang_of()`: heading chain for prose, key path for JSON/YAML/TOML, declaration for code, path alone for unlabeled. The splitter still does not dispatch — the header is one prepended string and costs a re-index to change, where a second chunker doubles the store and multiplies the eval matrix. Measured after through `discover.candidates()`, prose chunks carrying an `in:` line went 1% → 62% with **zero** headers not traceable to a real heading or key. It also subsumes the prose guard rather than sitting beside it: a header built from headings cannot be built from the declaration regex. Still unfalsifiable until `--corpus docs` exists, and that is said in the concept rather than left implied.
- **Fix + Creation**: [a new language costs nothing and cannot be filtered](constraints/a-new-language-costs-nothing-and-cannot-be-filtered.md), and `search` stops answering an unknown `lang` with silence. It narrowed the corpus to nothing and returned that as an ordinary empty result — indistinguishable from an honest answer, and the only refusal `mode` had from the start that `lang` never got. Same difflib "did you mean" as the config gate, and `tools.py` returns the string rather than raising so the hint crosses the MCP boundary. The concept beside it records why a `.zig` file already works: discovery is a denylist and `LANGS` supplies only a label, which is the half of the one-chunker decision a future tree-sitter proposal would quietly spend.
- **Update**: three gates the audit found named but not asserted. The chunker's boundary ladder is now tested at its top rung — and the obvious version of that test does not discriminate: with a two-paragraph budget, cutting on line breaks lands on a paragraph boundary anyway, so the fixture gives the splitter a spare line and demands it leave it on the table. The non-ASCII offset gate now walks to the character offset each chunk's `start_line` implies and requires the chunk to be there; concatenation, which was the whole check before, passes with every offset uniformly wrong. And one test runs at the real `CHUNK_CHARS=2000` rather than the small `size=` every other chunker test passes. The federation collapse moves out of a docstring: 135 repos behind 202 links, reproduced in a tmp tree rather than asserted against a registry that names client paths.
- **Update**: a restart test, and the switch it exposed. The one Phase 5 assertion never built — restart mid-build, queue drains, index converges without a full rebuild — now exists under its own `-m restart` marker, because `live.py` refuses to start or stop the daemon on purpose and that rule is worth more than the convenience of reusing it. It runs a `coderag serve` of its own on a private `CODERAG_STATE_DIR` and a free port rather than the installed unit: a restart of the real daemon sweeps every enabled project into the queue, so a fixture project would wait behind ~148 of them, and a test that writes to the real registry is how that file was lost twice. The observable that separates *resumed* from *rebuilt and converged to the same rows* is `progress.begin`, which is handed the work the pass found to do — the remainder after a resume, the lot after a wipe. Writing it surfaced that `systemctl --user start coderag` **is** the fleet index, so the live suite could not have a daemon up without starting an overnight run on the same card; `CODERAG_RECONCILE_ON_START=0` separates the two intentions and the 60 s tick still collects anything submitted.
- **Creation**: [CI runs no GPU and no live job](decisions/ci-does-not-touch-the-gpu.md). The workflow was inherited from the pre-rebuild tree and had been red on every push since 2026-08-18 — it linted `src/rag_search src/tests scripts`, none of which exist. Rewritten against the real tree. The self-hosted lanes are deleted rather than ported: their fork-PR path was already closed by construction, but a runner group is unavailable on a personal account, and `live-fast` ran on every push against the one card the fleet index is serialised on. Actions pinned to 40-char SHAs, `permissions: contents: read` stated, and the `-Werror` bundle step no longer `continue-on-error` — a gate whose failure is swallowed reports the same green as a passing one.

- **Update**: the bundle audited against the shipped engine rather than against the plan's table.
  All 21 paths named in a `resource:` key resolve, so nothing here describes deleted code — which
  was the failure mode of the v1 bundle and is the check `tests/test_okf_bundle.py` now holds. Nine
  concepts against a target of thirteen, and the four remaining gaps were closed as refusals, each
  after reading the file that would be restated: **a federation is discovery, never a merged index**
  (`federation.py` carries the resolved-path rule and the 202→135 dedup, `watch.py` carries the
  inotify-symlink trap, the joining-a-root Constraint carries the excludes union, and the sqlite-vec
  Decision carries why there is no merged index — four homes, none of them missing); **the two-tool
  MCP surface as an Interface** (the contraction and its refusals are a Decision already written,
  and the signatures are `tools.py`, so the file would be a schema copy that goes stale silently);
  **the registry lost update as a Defect** (`runbooks/restoring-the-registry.md` already carries the
  34-of-180 under its traps, and the runbook is where the reader who needs it arrives). The target
  of thirteen was an estimate made before the code was written; the count that matters is how many
  survive the gates, and four fewer is the gates working rather than the bundle being unfinished.
- **Deprecation**: `docs/` — all 70 files, the previous engine's architecture records and dated decisions. It was kept through the v1 bundle delete as the source the load-bearing numbers were transcribed from; every one of those numbers now sits in a concept here or in a module docstring, so the directory was a second prose plane describing code deleted in `365a235`. `knowledge/` is the only one. The content is in git history at `365a235^`, and a copy was zipped out of the tree rather than committed: a tracked binary duplicates what git already holds and is opaque to the `NAME_BAN` scan, which reads tracked *text*.
- **Creation**: [a model carries three things, and pooling is the one that fails invisibly](constraints/a-model-carries-three-things-and-pooling-is-the-invisible-one.md). Found while answering "why not the same Alibaba-NLP family for both stages" — `gte-modernbert-base` and both `bge` arms are CLS-pooled and `embed.py` hardcoded a masked mean, so three arms of the running bake-off were void. Written as a Constraint rather than an `embed.py` comment because its consequence is a procedure for adding an arm, and the person adding one is reading `tests/eval.py`.
- **Update**: `tests/eval.py` gains `jina-code`. The plan's claim that no code-specific embedder clears both the licence and official-ONNX filters was wrong: `jina-embeddings-v2-base-code` is apache-2.0 with an fp16 ONNX sibling, 768 dims and mean-pooled, so it is a drop-in arm. Missed by anchoring on the newer CC-BY-NC `jina-code-embeddings-0.5b`.
- **Creation**: [this host cannot produce an admissible latency number](constraints/this-host-cannot-produce-an-admissible-latency-number.md), written from a measurement taken while the bake-off ran: `GPU T.Limit` at −3, SM clock ~460 MHz of a rated 3090, 60 W drawn of 175 available, chassis 97 °C. Written as a concept rather than a `gpu.py` comment because it invalidates numbers in three other places — the sqlite-vec kill criterion, the live suite's recorded p50/p95, and every per-arm wall clock — and a comment beside `cool_down()` reaches none of those readers. The question that produced it was "should we use lighter models", and the honest answer is that the throttle costs ~6× against a shortlist spread of ~1 pp, so the model was the smaller term.
- **Update**: [the sqlite-vec decision](decisions/sqlite-vec-survives-only-because-search-is-scoped.md) gains the distinction between a criterion that is met and one that cannot currently be measured. Its whole argument is that the reversal condition is a number, and a number this host cannot produce would buy an ANN index to fix a fan.

- **Deprecation**: the v1 bundle — all 31 concepts — was **deleted** rather than deprecated in
  place. The `okf-knowledge-bundle` rule is augment-never-shrink, and this is the exception it does
  not cover: that rule governs a bundle whose subject still exists. Here the subject was deleted in
  `365a235`. Of the 31 files, the 7 Components, 2 Interfaces and 5 Defects described `graph/`,
  `sweeps.py`, `routes_chat.py`, `bounded_parse.py` and 16 HTTP routes that no longer exist, and
  every concept linked into `docs/architecture/federation-ops-and-invariants.md` and
  `docs/decisions/`, whose `HR#` rows were held by a test deleted with the suite. What survived
  would have been majority tombstone with entirely broken links. Git holds the originals; the
  measured numbers were transcribed forward into the concepts below before the delete.
- **Creation**: the bundle for the rebuilt engine. Written against shipped code, not intended work.
  Seven concepts, not the thirteen the plan targeted. The gap is the not-already-covered gate doing
  its job: this engine's module docstrings were written to carry their own why, so a concept
  restating one is the drift a bundle exists to prevent. Refused for that reason, each after
  reading the file: **GPU-only inference** (`gpu.py`'s docstring already names what each of the four
  assertions closes; the incident that proves the fourth is a Defect and is written), **the VRAM
  lifecycle** (`server.py` holds the 12.2 GB measurement, `os._exit` and the idle timer at their
  call sites), **indexing is always background** (`index.py`'s first paragraph), **the store's
  rowid contract** (`store.py`'s docstring, including the cascade that does not reach the virtual
  tables), **the publishable tree** (`tests/test_public_hygiene.py`, including why an unset ban
  fails), and **the derived query set** (`tests/eval.py`). What survived is what no single file can
  hold: evidence with rejected alternatives, a lifecycle spanning four modules, an incident, and
  two procedures.
- **Creation**: `tests/test_okf_bundle.py` — the bundle's gate. This repo has no CI and no git
  hooks, so pytest is the only gate there is, and a missing `okf` binary **fails** rather than
  skips. It also holds the two checks `okf check` treats as warnings and this bundle treats as
  errors: a link to a file that does not exist, and a `resource:` naming a path that does not — the
  second being exactly how the deleted bundle above ended up describing code that was gone.
- **Update** to `this-host-cannot-produce-an-admissible-latency-number`: the claim held, the
  cause did not. The card was never thermally saturated — it was pinned to 46% of its power budget,
  and the "SW Thermal Slowdown: Active, 330,867 s" that anchored the original diagnosis was
  `nvidia-smi`'s *lifetime counter* in microseconds read as instantaneous state. Amended rather than
  rewritten: the misreading is the reusable part, because an error carrying a unit and six
  significant figures does not read like a guess.
- **Creation**: `runbooks/restoring-dynamic-boost.md`. Written because the fix is two files Ubuntu's
  driver package installs nowhere systemd or D-Bus will look, and because the obvious verification
  probe (`nvidia-smi --query-gpu=power.limit`) reports `[N/A]` on a working system — a runbook that
  only listed the commands would have been re-derived and then disbelieved.
- **Refused**: a concept for the incremental persistence added to `tests/eval.py`. The reason lives
  at the call site in three words and the harness is not load-bearing; the durable lesson is already
  in `running-the-live-suite`, which says to read the deltas rather than the levels.
- **Update** to the same constraint, hours after the first: lifting the power cap moved
  the binding constraint rather than removing it. At 130 W the card draws 83–90 W and hits 87 °C,
  `SW Power Cap` goes Not Active and `SW Thermal Slowdown` goes Active. Both readings are correct at
  their own power limit, which is the durable lesson — **a throttle-reason diagnosis is only valid
  at the cap it was taken under.** The first amendment was right about that window and wrong to
  imply heat was never a constraint here.
- **Creation**: `tests/test_live_agent.py` — layer 3, the layer the pyramid names and this repo did
  not have. It asserts on the transcript, never on the answer: a `tool_use` block naming the server,
  a `root` in its arguments, and locations from the `tool_result` that resolve on disk. Asserting
  that a correct answer arrived would pass with the server switched off, because the session would
  open the file itself and be right.
- **Refused**: a concept for layer 3 and for the conformance suite. Both rationales are at their
  call sites and neither has an alternative that was rejected on evidence — the four gates stop at
  the first. The MCP revision the wire must negotiate (`2026-07-28`) is now asserted rather than
  written down, which is the form that cannot go stale silently.
- **Update** to `runbooks/restoring-dynamic-boost.md`, same day as its creation. The policy I
  shipped in it was one I wrote defensively — a DOCTYPE, explicit `deny` under the default context,
  extra allows under root — not the file the driver actually ships. Upstream's 595 policy is a
  216-byte form that *allows* `send_destination` in the default context, and the difference matters
  in both directions: a pre-2022 copy is [CVE-2022-31608](https://forums.developer.nvidia.com/t/nvidia-dbus-conf-lead-to-high-security-concerns/215303),
  and a malformed one makes dbus-broker refuse to start and takes logind and NetworkManager with it
  ([nixpkgs #545966](https://github.com/NixOS/nixpkgs/issues/545966)) — an unbootable machine from a
  file written to make a GPU faster. The runbook now validates the XML and proves the bus survived
  *before* the reboot that would otherwise discover it.
  Three smaller things the first draft had wrong or missing: the scriptable probe is
  `enforced.power.limit`, and `power.limit` reads `[N/A]` because it is the *Requested* limit and no
  software-settable limit exists on a mobile board — normal before and after the fix, not a
  Dynamic Boost symptom; the unit needs `SuccessExitStatus=1`, which the noble doc copy omits; and
  the honest expectation is **+5 to +25 W**, not the 80 → 175 W the numbers invite, because the rest
  of that gap is platform power mode and this machine has no `platform_profile` to reach it.
- **Creation** `decisions/progress-is-a-file-not-a-protocol-notification.md` and
  `decisions/the-thermal-pause-is-not-what-indexing-costs.md`. The first records two refusals with
  named alternatives: `notifications/progress` needs a `progressToken` on a live request and `index`
  returns immediately by design, and the Tasks extension is poll-based — which is the shape `index`
  already has, so adopting it adds a second status surface for a semantic already exposed. The
  second is a profile that contradicted the question that prompted it: the thermal cooldown is one
  sample in 2,359 and the forward pass is 80.7%, so *keeping* the governor is the decision, and the
  batch-size finding (7.63 chunks per call against a ceiling of 128) is recorded where the next
  person looking to speed up indexing will meet it.
- **Refused**: a concept for `progress.snapshot`'s ETA algebra. The reasoning is three lines at the
  call site and there is no rejected alternative — the "clever" form reduced to the naive one.
- **Update** `decisions/the-thermal-pause-is-not-what-indexing-costs.md` the same day it was
  written. Its profile stood, but the cause it inferred did not: batching the embed call across 64
  files instead of one per file measured **2–3% slower** on an ABBA run with a warm model
  (32.7/33.0 s against 32.0/32.0 s), and was reverted. Vectors were equivalent — min cosine
  0.9999997 — so the result is throughput, not correctness. A profile localises time; it does not
  name a cause, and eight minutes of A/B beat a plausible reading of a flame graph.
- **Creation** `defects/chunks-are-cut-before-the-model-sees-them.md`, which is what the refuted
  experiment found instead. Padding waste at batch 128 is 0.0% because every chunk arrives at
  exactly `EMBED_MAX_TOKENS`; untruncated, they run p50 904 / p95 1373, so 70% of chunks are cut and
  27% of tokens never reach the dense lane. Two arms (`gte-win1536`, `gte-chunk1000`) are in flight
  to decide whether it costs recall, and both constants are frozen until they land.

## 2026-08-20

- **Creation** `decisions/the-header-is-the-path-and-nothing-else.md`. The code 2x2 completed all four
  cells: the path arm is worth **-0.1233 recall@1 / -0.1700 recall@10**, and the derived line is an
  exact 16/16 discordant tie with path on (p = 1.000) and *negative* with path off, where
  `derived-only` scores 0.1267 against `no-header`'s 0.1467. Deleted `CHUNK_HEADER_DERIVED`, its
  stamp field, four helpers, five regexes and the per-type dispatch; `chunk.py` 248 -> 137 lines,
  `CHUNK_ALGO` 2 -> 3.
- **Refuted** `decisions/the-header-dispatches-on-type-the-splitter-does-not.md`, by the
  falsification condition it wrote for itself: *"If they lose or tie, the path-only prose header and
  the one-splitter decision come back strengthened."* They tied.
- **Refuted the explanation, not just the arm.** A redundancy census over 876 code chunks put the
  `imports:` line at **15.3%** overlap with the body it labels -- 85% novel tokens -- so the
  embedder was handed new text and did not use it, and used it *against* itself when the path was
  absent. Dilution, not redundancy. That generalises where redundancy would only have indicted these
  particular regexes and invited a fourth attempt.
- **Recorded and not closed**: the path arm's magnitude on docs is soft. 48.6% of docs eval queries
  have their heading echoed in the positive's filename, and BLAgent (arXiv:2605.17965), the external
  corroboration at +17.0 pp, presents query-document string identity as its mechanism approvingly.
  The code corpus has no such echo and pays -0.1233 anyway, so the decision stands on code. Re-scoring
  the docs path arm on the low-slug-overlap stratum is zero-GPU and still owed.


## 2026-08-20

- **Update** `decisions/the-embedder-is-settled-by-a-tie-break.md`, a day after it went `stable`:
  `EMBED_MODEL` to `Alibaba-NLP/gte-modernbert-base`, `EMBED_POOLING` to `cls`, `EMBED_ONNX_FILE` to
  `onnx/model_fp16.onnx`, both prefixes to `""`. fp16 was the tie-break's first criterion, so it was
  verified rather than assumed — the whole `-m gpu` lane (30 tests) passes on the fp16 export.
- **Update** `test_the_two_prefixes_produce_different_vectors`, false by construction once the
  prefixes are blank, with one that asserts both halves: the sides agree under blank prefixes and
  stop agreeing under a prefix. Deleting it would have deleted the assertion that `side` is
  load-bearing.

## 2026-08-20

- **Creation**: [a cancelled task group cannot reach a shielded thread](defects/a-cancelled-task-group-cannot-reach-a-shielded-thread.md).
  The stop hung again after the previous fix. `timeout_graceful_shutdown` bounds the connection wait
  and nothing else; the 90 s was the lifespan shutdown waiting on a plain-`def` tool running under
  `anyio.to_thread.run_sync`'s shield, which the session manager's cancel cannot reach — and the tool
  then restarted the threads the finally had just stopped. Fixed with `SHUTDOWN_DEADLINE_S = 15` and
  a `threading.Timer` armed as the first statement after `STOPPING=1`, in the inner context, strictly
  before the session manager is entered.
- **Update** [a deliberate stop was indistinguishable from a crash](defects/a-deliberate-stop-was-indistinguishable-from-a-crash.md),
  by running its own test: it fails, `-15 != 0`, and has presumably never passed. uvicorn's
  `capture_signals` restores the handler it replaced and re-raises the caught signal before
  `uvicorn.run` returns, so `_shutdown_exit` was dead code and the CUDA-134 protection the module
  docstring calls a lifecycle fact was not in force. Not a production failure — systemd's
  `is_clean_exit` treats a daemon killed by SIGTERM as clean — but every `returncode == 0` assertion
  in the restart suite was unreachable. One line: install our own SIGTERM handler before
  `uvicorn.run`, so ours is the one uvicorn restores into.
- **Measured** the unbounded stop at **5.7 s** with a cold-load `search` in flight, with the daemon's
  own log showing both ONNX sessions being built after `"StreamableHTTP session manager shutting
  down"`. The first version of the new test asserted 25 s and passed with the deadline neutralised —
  decoration. It now runs its daemon at a 2 s deadline and asserts under 4.5 s, between the two
  numbers, because at the shipped 15 s neither is reachable.

- **Creation**: [the search unit is the caller's own workspace](constraints/the-search-unit-is-the-callers-own-workspace.md)
  and [an unflagged project stayed fully searchable](defects/an-unflagged-project-stayed-fully-searchable.md).
  The scoping downstream of `root` was already right — one level of federation, no fleet-wide mode —
  and the hole was that `root` is a string the model writes. The boundary now comes from the client's
  own `roots` through the SDK's `ListRoots` resolver, which the framework fills and the schema never
  shows. `search`'s own predicate went from "the row exists" to registered **and** enabled **and**
  indexed, which is two live bugs in one line.
- **Verified over the wire**, against a real daemon at the fail-closed default: round one answers
  `input_required` with `{"coderag.scope:_ask": {"method": "roots/list"}}` and an opaque
  `requestState`; the retry carrying the pin is served; the retry carrying a *different* pin is
  refused. `live.Rpc.modern_tool` is that round-trip, and `tests/test_live_protocol.py` asserts both
  arms plus the discriminator — an unpinned call has to fail for a different reason, or "the pin
  bounded the search" is unearned.
- **Recorded** SEP-2577: `2026-07-28` deprecated `roots` in the same revision that made it reachable
  from a stateless server. Twelve-month floor, removal eligible no earlier than 2027-07-28, and the
  successors the spec names are the two things this rejected — a tool parameter and server
  configuration. Not a reason to change course now; a reason to have written the date down.
- **Shipped the flag open in the unit and closed in the code.** Claude Code negotiates the
  `2026-07-28` era for `http` behind a default-off flag, so an unpinned client would be refused
  outright. `scope.enforce` logs the pinned root count on every call; journald decides the flip, and
  the log line goes when the unit line does.
- **Not changed**: the watcher still watches `enabled_projects()`, a superset of the indexed set.
  inotify has no replay, so tightening it for symmetry with the search gate would lose every write
  during a project's first pass.
- **Creation**: [a delete is lost while a project is still settling](defects/a-delete-is-lost-while-a-project-is-still-settling.md), `status: open`. The one red test in the live lane, and not caused by the scoping change beside it. Same tree, same shape, same fleet: a delete a minute after registration survives 300 s, and one after a 90 s quiet period leaves the store in 10 s. Delivery, `_dispatch` and the scoped delete branch are each now held by a test rather than by an argument. Two probe designs inverted the answer before that — polling `index` submits a full pass and repairs what it measures, and `/healthz`'s `completed` counter is fleet-wide, so a `+1` never attributes to the tree under test.
- **Update**: two tests for paths that had none. `test_a_delete_through_a_symlink_still_reaches_the_queue` is the twin of the write test that already existed — the delete direction is the one that fails silently, and its file is created before the watcher starts so only the removal can satisfy it. `test_a_scoped_pass_removes_the_file_the_watcher_named` covers `index_project(project, paths)`, the call shape the watcher actually makes, where the content-hash diff never runs; the pre-existing delete test only ever drove the full walk.
- **Update**: `test_a_write_reached_only_through_a_symlink_is_noticed` can pass without the watcher firing. The fixture waits on the **root's** chunk count and writes into the **member**, whose own first walk has not run and sweeps the file in. Written the awkward way and still not awkward enough; the member's store has to go quiet first.
- **Update**: [a delete is lost while a project is still settling](defects/a-delete-is-lost-while-a-project-is-still-settling.md) is `resolved`. `tools.index_project` re-armed the whole watch set on every `index` call, registry change or not — a teardown and rebuild of ~120,000 inotify watches, 5.4 s measured over 151 projects, plus up to `WATCH_POLL_MS` before the loop even sees the flag. inotify replays nothing, so each call opened a ~10.4 s blind window, and the failing test polled `index` throughout its own. The 90 s-idle probe made no such call, which is the entire difference between the two rows in the table. `rearm_if_changed` is now the only entry point and `rearm` is deleted; the live results file went from a 300 s timeout to green in 12 s from a cold registration.
- **Creation**: [the CPU side of an indexing pass is already flat](constraints/the-cpu-side-of-an-indexing-pass-is-already-flat.md), and §3's whole point was to find out. Six arms, three repetitions each, store deleted and `store.close_all()` called between them — without the close, the cached sqlite handle keeps the unlinked file alive and the first run reported three 0.03 s passes over 287 chunks it never built. Nothing cleared its pre-committed threshold, `MALLOC_ARENA_MAX=2` cost 27% more CPU, and the run is not void (baseline spread 0.09 CPU-s against a best delta of 0.19). The `CODERAG_ORT_*` constants written to measure it are deleted; `tests/cpu.py` keeps the env-only arms. One thing the harness did not deliver: every thread's `comm` is `python`, so per-thread attribution collapses and only the thread count discriminates.
- **Creation**: [a rootless call with no pin told the caller to index their home directory](defects/a-rootless-call-told-the-caller-to-index-their-home-directory.md), `status: resolved`. The last red live test: the agent's `search` came back as `{"error":"$HOME is not indexed -- call index(root=$HOME) first"}` because `scope.default_root` fell back to `Path.cwd()`, and the daemon's cwd is the operator's home. It was only visible because the assertion was widened to dump the payload — an error envelope, an empty result set and an unparseable shape all reduce to `[]` at that assertion and are three different bugs. `default_root` now raises `ScopeError`, which both tools already return as an envelope; `index_project`'s `resolve` moved inside its `try` so the raise cannot escape as a transport error.
- **Creation**: [`watching` answers for one project, and arming is asynchronous](constraints/watching-is-per-project-and-arming-is-asynchronous.md). Falling out of the delete-defect fix: `watch.watching()` is thread liveness, true for every project at once the moment the thread starts, and the live fixtures read it as "safe to write now". `watch.armed(project)` is membership in the armed set. `tests/test_live_federation.py` asserts it under `until(...)` rather than synchronously after `index` returns, because the rebuild is what it is waiting for.
- **Creation**: [the watcher woke the indexer for files git ignores](defects/the-watcher-woke-the-indexer-for-files-git-ignores.md), `status: resolved`. Found by watching the fleet index rather than by a test: the queue grew while the worker drained it, and the journal carried `3 changes detected` every four seconds with nobody typing. A gitignored build cache in one repo. `discover.indexable` is shared so the two cannot drift, but gitignore was never in it -- the indexer gets that from `git ls-files --exclude-standard`, one layer above the shared predicate. `discover.git_ignored` is one `check-ignore` per project per batch, subtracted in `_dispatch`.
- **Update**: `armed` was set where the loop *decides* to rebuild, not where the watches exist, so it answered true through the whole blind window and the live write test went back to a 300 s timeout -- the same failure the field was introduced to prevent. Two names now: `_intent` for the change comparison, `_armed` after the watcher's first yield. Reproduced without symlinks first (a plain write into a freshly armed project was never indexed), which is what ruled out the gitignore filter shipped in the same session.
- **Creation**: [a project whose directory was gone could not be turned off](defects/a-vanished-project-could-not-be-turned-off.md), `status: resolved`. `index_project`'s `is_dir` refusal sat above the unflag branch, so the row an operator most needs to disable was the one row the surface could not act on — disabling returned an all-`None` result and the row stayed enabled. Two of them were the whole of `/healthz`'s `failed: 2`, retried and logged with a traceback at every start. The regression test asserts the project is absent from `enabled_projects()`, not that the row reads `enabled: False`: `federation.unregister` drops a member nothing else claims out of the registry entirely, and the first version of the assertion failed on that.
- **Creation**: [one live test skipped the disable-don't-prune teardown](defects/a-live-test-skipped-the-disable-teardown.md), `status: resolved`. The first reading blamed the policy and measurement refused it — 88 disabled rows, zero with a dead path, zero under `/tmp`. One test in eleven, and its disable now sits in a `finally`. `conftest.fleet_unchanged` counts the daemon's enabled rows around every `live` module, per module so the red names the file; the count comes from `/healthz` because reading `projects.json` from a test is the shape of the registry-wipe incident.
- **Creation**: [doctor walked rows to stores and never stores to rows](defects/doctor-only-walked-the-registry.md), `status: resolved`. 144 stores at 436 MiB with no row to be found from, under the check five consecutive verification blocks quoted as proof of no orphans. The reverse walk compares against **every** row, not the enabled ones — unflagging keeps the store, so walking enabled rows only would have reported all 88 disabled projects. The generator is `unregister`, which is why six more appeared during this run's own live and restart lanes, and why `doctor --prune` is a flag rather than a runbook of `rm`.
- **Creation**: [the pin rollout needed a reason, not a count](decisions/the-pin-rollout-needed-a-reason-not-a-count.md). `workspace pin: 0 root(s)` 4498 times against 23 real pins reads as a guard that never fires; the count cannot separate `_ask`'s two branches, which is the only question the flag turns on. `_ask` logs the branch now. A `search` from a Claude Code session today produced a real pin and a correct out-of-workspace refusal, and neutering `scope.enforce` against the live daemon turned `test_the_client_roots_pin_arrives_and_bounds_the_search` red with the refusal degraded to `is not indexed` — the discrimination check the criterion needed. `REQUIRE_CLIENT_ROOTS` stays `0`: four profiles are unchecked, and byte-identical MCP config is what they already look like.
- **Creation**: [what a root and 135 federated members cost](constraints/what-a-root-and-135-members-cost.md). p50 1.46 s, 91% of one nice-10 core, 2.31 GiB anon, 118 of 120 hits attributable to members. Two metrics quoted in four plans are wrong and are corrected here: `memory.events high` counts page-cache reclaim, so 404 of them cost 26.8 ms of stall and `high == 0` fails on the kernel working; and `pids.current` reads 76 fresh against 99 warm, so the "99 vs 77 threads" alarm was a fresh-versus-warm comparison.
- **Update**: `gpu.py`'s docstring said GPU-only is "asserted at four points". Three run in the daemon. `check_placement` has no production call site deliberately — reading it costs a profiled batch through both models, and what it guards changes on a model swap — so it is a gate held by `-m gpu`, which CI cannot run by decision. The docstring says that now, because the previous wording let the daemon's green read as covering it. `CLAUDE.md`'s 300-line ceiling likewise names `src/coderag/` and says four test files are over it, rather than being quoted as true in a fifth verification block.
- **Update**: [the pin rollout needed a reason, not a count](decisions/the-pin-rollout-needed-a-reason-not-a-count.md), second amendment: `CODERAG_REQUIRE_CLIENT_ROOTS` is flipped and the unit line is deleted. The open item was "a day of journald with `clientInfo` logged", and a day would not have closed it: the instrumentation shipped with three defects that made its own criterion unreadable ([empty root answers](defects/empty-root-answers-from-clients-nothing-identifies.md), amended). `params.clientInfo` is a serialization alias, so 197 of 197 lines named nobody, and the unit test mirrored the wrong name back; `client_params` is synthesized only when info *and* capabilities arrive, so the caps-less branch could never be named from a session; and `_ASKED` was a `ContextVar` written by a **sync** resolver, which `anyio.to_thread.run_sync` runs in a discarded `copy_context()` — so "answered empty" and "no resolver ran" were one log line. Replaced by a census of a closed population: five profiles, five distinct peer pids, all `branch=asked roots=1`, answered-empty zero. Attribution now reads this request's `_meta` and the peer's pid from `/proc/net/tcp` ([who called is a per-request read](constraints/who-called-is-a-per-request-read.md)), and `pinledger` gives the count a denominator journald's seven days cannot.
- **Update**: the flip refused one real caller, and the census is what caught it — the layer-3 agent test's own `claude -p`, on `2025-11-25`, `branch=legacy-path`, which cannot carry a pin at any setting. Four candidate causes were tested one at a time against the running daemon; the one that moves the era is `--strict-mcp-config`, and a fresh config dir, the model, `--verbose` and a git-repo cwd each make no difference. `Rpc.tool` now pins at the `root` every one of its 37 call sites already names, so the live lane runs under the flag it ships rather than under `CODERAG_REQUIRE_CLIENT_ROOTS=0`. Two assertions changed because the flip made their premises false: an unpinned call is refused rather than answered, and the agent session is no longer required to write a `root` — with a pin, `root=""` *is* the workspace.
- **Update**: [the search unit is the caller's own workspace](constraints/the-search-unit-is-the-callers-own-workspace.md), amended — the ancestor arm `enforce` has always sanctioned was computed by nothing. `default_root` looked `roots[0]` up verbatim, so 86.6% of 14,098 sessions on this machine were told their own project was not indexed while pinned one directory inside it; 7.2% pinned a registered root and worked. `registry.enclosing` walks up to the nearest enabled row, longest match first, borrowing `watch._owner`'s rule because a member can live under its root's tree. The three refusals that had to survive each have a test: no registered ancestor, a disabled ancestor, and the cross-workspace refusal. A worktree now resolves to its main checkout, which means its uncommitted edits are not in the results — disclosed at the time by a SessionStart hook in `claude-code-workflows` (`hooks/claude/coderag_reach.py:70`) and not by the reply, which reached Claude Code and no other client; see the 2026-08-22 entry.
- **Creation**: [membership is a directory symlink at depth four or less](decisions/membership-is-a-symlink-and-the-depth-limit-is-measured.md). Both halves came from the deleted v1 engine with no argument anywhere — `federation.include` was absent rather than refused, and `MAX_DEPTH = 4` silently narrowed an any-depth walk. The limit is measured now: on the one root that federates, links sit at depths 2/3/5/6/7/8 (142/59/14/74/3/2), and of the 93 below the cut **zero** are lost — each resolves to a target already reached by a shallower link, or is excluded, or is not a project. Kill criterion stated, because a probe that cannot tell "beyond the limit" from "actually missing" would have read the same either way.
- **Update**: discovery had no clock. `federation.discover` was reachable only from an explicit `index` call, so a symlink added afterwards was never picked up, and `reconcile_all` had no caller at all despite `index.py`'s docstring naming the tick. `federation.sweep` + `server._sweep` run hourly on `SWEEP_EVERY_S` inside the existing tick — not per tick, because a re-arm is ~120,000 inotify watches at 5.4 s over 151 projects, so it goes through `rearm_if_changed`. The sweep only ever adds: a vanished link and a briefly-unmounted target are the same observation, and the pruning version of that rule wiped the fleet registry once. `index.submit` collapses an identical whole-project walk already queued, because the hourly pass re-enqueues all 149 rows; the dedup reads `_queue.queue` under its own mutex rather than a shadow set, since anything that drains the queue and is not the worker leaves a shadow set claiming a job that no longer exists and silently drops every later submit.
- **Creation**: [the daemon records its own work, and the library that filled the journal goes quiet](decisions/the-daemon-records-its-own-work-and-the-library-goes-quiet.md). The journal inverts: 3,800 of 5,912 lines in 24 h are `watchfiles` counting changes it does not attribute, and the 454 index passes the registry recorded in the same window produced zero lines. `_drain` threw away everything `index_project` returned, `_dispatch` dropped an event in three bare `continue`s, and `_sweep` was silent whenever it claimed nothing, which is nearly always. `runs.jsonl` carries `index`, `watch`, `sweep`, `arm` and `sched` rows, and `ledger.py` is the append-rotate-read block `pinledger` and `searchledger` each held a copy of. `watchfiles` drops to `WARNING` below `CODERAG_LOG_LEVEL=DEBUG`, so this section's net effect on the journal is quieter, which is how it honours [the dated refusal](defects/a-failure-that-resolved-itself-left-no-trace.md) rather than arguing with it. An empty watch batch records nothing: a timeout yield is the common case.
- **Update**: [the search unit is the caller's own workspace](constraints/the-search-unit-is-the-callers-own-workspace.md), sixth amendment amended. Dropping `scope.enforce` from `search` dropped two rules, not one: the containment check that was meant to go, and the `REQUIRE_CLIENT_ROOTS` refusal that was not. A client declaring no workspace could name any indexed row and read it, which is the era the flag rollout closed. `scope.require_pin` is that half, split out and called by both surfaces. Caught by the live lane rather than by a unit test, because the flag is read from the daemon's environment. `test_5_a_member_is_nameable_directly_now` also asserted a narrowness `federation.unit` deliberately does not give -- a member widens to its root's federation, on the fourth amendment's reasoning that a member alone is 1 project of 143 -- so it asserts the member ranks first instead. `unit`'s docstring justified the widening partly on the `enforce` refusal this amendment removed, and now stands on the reason that survives.
- **Creation**: [a cached SQLite handle is not free at rest](constraints/a-sqlite-handle-is-not-free-at-rest.md). The daemon sat at 3.56 GiB against `MemoryHigh=4G` with 1.39 GiB swapped, and the GPU side was innocent — 16 idle unloads in 30 h, `malloc_trim` with each, and RSS unmoved. 1,506 `index.db` handles over 415 stores on four threads, at 2.12 MB of filled page cache each, is 3.2 GB of it. Three settings and one deletion: `SQLITE_CACHE_KIB=256`, `conns.reap_idle` on the scheduler tick at `STORE_IDLE_S=600`, `THREAD_LIMIT=8` on the anyio pool, and the second fan-out at the end of `search` that opened every member store again for a display field the registry row already carries. The reap also closes a handle whose store is unlinked, which is what the 15 fds pinning 4 deleted fixture stores needed. `conns.py` is a new module rather than more of `store.py`: the split is what keeps `store.py` under the 300-line ceiling, and connection lifetime is a different subject from the rowid contract.

- **Creation**: [the watchdog ping rode the job loop](defects/liveness-rode-the-job-loop.md), `status: resolved`. The ping was the first statement of `_tick` and `WatchdogSec` was `SCHEDULER_TICK_S * 3`, so the deadline measured the loop it rode. Five kills at exactly 180 s at load 22, no deadlock in the dumps, and 334 s clean at load 10: slow, not dead. `watchdog.py` is a new module rather than more of `server.py`, which sits at 286 lines against the 300 ceiling. Its thread only sleeps and sends a datagram, at `WATCHDOG_USEC / 3` read from systemd's own environment rather than from config. The gate is not unconditional: `stall()` wants a live scheduler thread that called `beat()` inside `SCHEDULER_STALL_S`, 900 s against a worst cycle near 15 s.

  `WatchdogSec` is `WATCHDOG_SEC=90` now, because deriving it from the tick would let a longer tick buy a wedged process more time. The withheld ping logs once per reason and reaches `/healthz` under `scheduler_errors.heartbeat`. A restart inside 90 s erases the journal context that would explain it.

- **Creation**: [the CLI ran the search in its own process](defects/the-cli-loaded-a-second-model-beside-the-daemon.md), `status: resolved`. `cli._search` called `search.search`, so every invocation built a CUDA session beside the daemon's -- 3114 MiB against a 16303 MiB card the daemon already holds 7660 MiB of. `_GPU_INFER_LOCK` could never help: it is a `threading.RLock` and the two sessions are in different processes. The failure lands at `cublasCreate`, which is handle setup rather than a forward pass, so `embed`'s half-batch OOM retry cannot fire either. `daemon.py` is a client for the running daemon, and the local search stays as the fallback for the case where nothing answers and nothing shares the card. No relaxation of `scope.require_pin` was needed: the `2026-07-28` era carries `roots/list` inside `InputRequiredResult`, so the client answers the pin in a second POST with the root the caller typed. Two deltas are accepted rather than papered over -- the tool strips `trace` and rewrites `hint` -- and one is not: the tool returns a failed search as an `error` envelope, so `_search` re-raises it, because printing an empty result set at exit 0 is a false zero the caller cannot see. Measured after: 592 ms per semantic search against about 7 s, and one compute process on the card.

- **Update**: [a SQLite handle is not free at rest](constraints/a-sqlite-handle-is-not-free-at-rest.md)
  named `store.session()`, and the function is `conns.session()`. The concept also claimed two rules
  the suite did not check. `tests/test_conns.py` covers both, plus the cross-thread reach that makes
  a reap possible at all, and every arm was run against the behaviour removed.

- **Creation**: [a watch batch carries one event](constraints/a-watch-batch-carries-one-event.md).
  `runs.jsonl` recorded 2,085 index passes in an hour and 303 in 15 minutes, every row
  `reason: watch`, `paths: 1`, `queued_ms: 0.19`. A pass hashes the whole tree to find what moved,
  so a one-line save walks 10,408 files. `WATCH_DEBOUNCE_MS` cannot reach it: that is the Rust
  batch window, and these events arrive 5 to 20 s apart. `quiet.py` holds a job per project for
  `WATCH_QUIET_MS`, 15,000 ms, and each further event widens the paths and restarts the countdown.
  It is a new module for the same reason `conns.py` was: `index.py` sits at 291 lines against the
  300 ceiling. An explicit `index` call still submits with no delay and **takes the held job with
  it**, so it never runs a narrower pass than the one it displaced. Two surfaces exist because the
  hold is off the queue and `queue_depth` cannot see it: `index.status()` publishes `held`, and
  `tools._pending` counts a held project, without which `index` answers 0 to "I saved a file and
  nothing happened". Losing a held job at shutdown is safe here and nowhere else, because staleness
  is one content-hash diff and the hourly reconcile finds it. This is also why the idle half of
  `conns.reap_idle` had never fired live: the indexer thread was never quiet for `STORE_IDLE_S`,
  measured as 1,291 to 2,065 open handles over 1,400 s with no reap. Eight mutations were run and
  each reds exactly one arm; the two that were uncaught on the first pass -- the watcher dropping
  the delay, and `_pending` ignoring the hold -- are the two arms added for them.

- **Update**: [a SQLite handle is not free at rest](constraints/a-sqlite-handle-is-not-free-at-rest.md),
  amended: the reap was unobservable. `_guarded("stores", conns.reap_idle)` discarded the returned
  count and logged nothing, so `/proc/<pid>/fd` was the only evidence one ever ran. Confirming it
  live took three watches and a 361-project search to isolate a thread certain to go idle -- it
  fired at 660 s and closed 363 handles. `server._reap_stores` records a `reap` row with `closed`
  and `open`, silent when it closes nothing, on the rule an empty watch batch already follows.
  `open` needs `conns.open_count` because the number spans every thread. Two facts the row does not
  fix are written down instead: `MemoryCurrent` rose across that reap, since a closed handle returns
  its page cache to the allocator and `malloc_trim` runs only on model release; and the indexer
  thread calls `connect` about every 18 s, so it never reaches `STORE_IDLE_S` while any watched
  project is written.
