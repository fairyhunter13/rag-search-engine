{
  "mcpServers": {
    "opencode-search": {
      "command": "opencode-search",
      "args": ["daemon", "bridge-stdio"],
      "description": "GPU-accelerated local semantic code search. Index your projects and search with natural language.",
      "alwaysAllow": [
        "list_indexed_projects",
        "project_status",
        "search_code"
      ]
    }
  }
}
