# GPU-only defaults for RTX 5080 Blackwell (Ubuntu 24.04, CUDA 13.0, Driver 580.142)
# Prevent CPU fallback for embedding/inference — GPU or FAIL.
OPENCODE_GPU_REQUIRED=1
OPENCODE_DISABLE_TENSORRT=1
CUDA_VISIBLE_DEVICES=0
OPENCODE_ONNX_PROVIDER=cuda
