# Dashboard chat: Claude Code (claude-haiku-4-5) only — codex support removed.
# Overrides any inherited RSE_QUERY_LLM_* (e.g. stale codex env from the shell session).
[Service]
Environment=RSE_QUERY_LLM_PROVIDER=claude
Environment=RSE_QUERY_LLM_MODEL=claude-haiku-4-5
