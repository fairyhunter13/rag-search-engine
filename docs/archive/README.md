# Archived planning documents

These are historical planning artifacts from the May 2026 Python migration.
They are kept for context only; the live code and tests under `src/` are the
authoritative source of truth. References to `index_project` / `search_code`
in these files refer to the pre-v2 5-tool MCP API and are intentionally not
updated — see [docs/INSTALL.md](../INSTALL.md) for the current 7-tool API.
