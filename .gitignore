# Rust
indexer/target/

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Stale feature-run artifacts (prerelease / QA / HPV systems removed in Phase 39)
.prerelease_report.*
.qa_report.*
.opencode_verify_report.*
.opencode_verify_state.*
.hpv_proof.*

# docgen auto-generated docs/ hierarchy (written into each repo by run_docgen — not source)
/docs/

# Local scratch diagnostics (never commit; may contain local device paths)
scratch_diag.py
scratch_*.py

# Secrets / local env overrides (use ~/.config/opencode-search/env instead)
.env
