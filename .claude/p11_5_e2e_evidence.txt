P11.5 MODEL-PINNED MULTI-PROFILE E2E EVIDENCE — Jun 14 2026
============================================================

configure_integrations.py --check: 15/15 targets ok, 0 need attention

BINARY DRIVE RESULTS
--------------------

(A) claude --model claude-haiku-4-5 (default profile, no CLAUDE_CONFIG_DIR)
  Model confirmed : claude-haiku-4-5-20251001
  Tool call order : Skill, Skill, ToolSearch, mcp__opencode-search__overview, mcp__opencode-search__search
  Verdict         : PASS — MCP tools called FIRST before any grep/Read; bridge connected; overview
                    returned real project data; prompt obeyed the search-first rule.

(B) codex exec -m gpt-5.4-mini --dangerously-bypass-approvals-and-sandbox
  MCP calls       : 23 calls to opencode-search (search x21, ask x2)
  Result sizes    : 68963 / 84893 / 278783 / 589965 bytes (real non-empty data)
  Answer excerpt  : cited acme_promo_db tables: vouchers, voucher_contents, voucher_costs,
                    voucher_rules, gwp_campaigns, gwp_rules (real indexed acme-promo-be data)
  Verdict         : PASS — opencode-search MCP called spontaneously FIRST; real round-trips;
                    answer cites REAL astro paths.

(C) claude account1 / account2 — CONFIG-VERIFIED (user decision: headless /login not required)
  --check shows: ~/.claude-account1/.claude.json and ~/.claude-account2/.claude.json each carry
  mcpServers.opencode-search (canonical command/args/env); CLAUDE.md canonical (index(x3 + RESILIENCE).
  Not binary-driven (profiles not logged in headlessly; gap is auth, not config).

(D) hermes + opencode default/personal — CONFIG + PROTOCOL verified
  --check shows all 4 targets (hermes/config.yaml, hermes/agent_system_prompt,
  opencode-default/opencode.jsonc, opencode-personal/opencode.jsonc) canonical-in-sync.
  Bridge launch command is IDENTICAL across all 7 trees (canonical.py CANONICAL_MCP_COMMAND/ARGS/ENV).
  P11.3 L3 stdio round-trip proves the bridge end-to-end (initialize -> 5 tools -> tools/call on real
  astro); config-identity + L3 = hermes/opencode MCP integration proven.
